# 视频生成：dashscope（阿里云可灵，需密钥）| slideshow（本机伪文生视频，零视频 API 费）
VIDEO_PROVIDER=dashscope

# --- DashScope ---
DASHSCOPE_API_KEY=

# --- Slideshow（VIDEO_PROVIDER=slideshow）---
# 需本机已安装 ffmpeg（PATH 可执行）。默认无声 MP4；输出在 public/generated-video/<jobId>/out.mp4
# 单进程内存任务队列，重启即丢失；不适合 Vercel Serverless。
# 耗时粗估：每页幻灯片约 0.5～2 秒渲染 + FFmpeg 数秒～1 分钟（页数多更久）；前端轮询约最长 4 分钟。
SLIDESHOW_SEC_PER_SLIDE=4

# 扩写 API（app/api/expand）
OPENCODE_API_KEY=
